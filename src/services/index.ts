/**
 * The Cultural Map — Services
 * Replace with Supabase client and API services.
 */

// Placeholder: import { createClient } from '@supabase/supabase-js';
// export const supabase = createClient(URL, KEY);

export {};
